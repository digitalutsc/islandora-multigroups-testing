langcode: en
status: true
dependencies:
  config:
    - group.type.manager_user
    - node.type.article
  module:
    - gnode
    - node
id: manager_user-group_node-article
label: 'Manager User: Group node (Article)'
description: 'Adds <em class="placeholder">Article</em> content to groups both publicly and privately.'
group_type: manager_user
content_plugin: 'group_node:article'
plugin_config:
  group_cardinality: 0
  entity_cardinality: 1
  use_creation_wizard: true
