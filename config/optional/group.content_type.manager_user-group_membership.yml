langcode: en
status: true
dependencies:
  config:
    - group.type.manager_user
  module:
    - user
id: manager_user-group_membership
label: 'Manager User: Group membership'
description: 'Adds users to groups as members.'
group_type: manager_user
content_plugin: group_membership
plugin_config:
  group_cardinality: 0
  entity_cardinality: 1
  use_creation_wizard: false
