langcode: en
status: true
dependencies:
  config:
    - group.type.manager_user
    - node.type.page
  module:
    - gnode
    - node
id: manager_user-group_node-page
label: 'Manager User: Group node (Basic page)'
description: 'Adds <em class="placeholder">Basic page</em> content to groups both publicly and privately.'
group_type: manager_user
content_plugin: 'group_node:page'
plugin_config:
  group_cardinality: 0
  entity_cardinality: 1
  use_creation_wizard: true
