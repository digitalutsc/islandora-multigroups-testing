langcode: en
status: true
dependencies:
  config:
    - group.type.general_user
    - media.type.image
  module:
    - groupmedia
    - media
id: general_user-group_media-image
label: 'General User: Group media (Image)'
description: 'Adds <em class="placeholder">Image</em> content to groups both publicly and privately.'
group_type: general_user
content_plugin: 'group_media:image'
plugin_config:
  group_cardinality: 0
  entity_cardinality: 1
  use_creation_wizard: true
