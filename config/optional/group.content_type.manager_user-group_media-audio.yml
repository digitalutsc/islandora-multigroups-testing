langcode: en
status: true
dependencies:
  config:
    - group.type.manager_user
    - media.type.audio
  module:
    - groupmedia
    - media
id: manager_user-group_media-audio
label: 'Manager User: Group media (Audio)'
description: 'Adds <em class="placeholder">Audio</em> content to groups both publicly and privately.'
group_type: manager_user
content_plugin: 'group_media:audio'
plugin_config:
  group_cardinality: 0
  entity_cardinality: 1
  use_creation_wizard: true
